spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://url/DataBase?setUnicode=true&characterEncoding=utf8&useAffectedRows=true
    username: XXXXXXX
    password: XXXXXXX

mybatis:
  type-aliases-package: com.auggie.student_server.entity
  mapper-locations: classpath:mapper/*.xml

server:
  port: 10086